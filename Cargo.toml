[package]
name = "destr"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.18", default-features = true }

# Compile bevy fixtures fast enough to actually run.
[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = 1